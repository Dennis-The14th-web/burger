<style>
    body {
        background-image: url(https://images.unsplash.com/photo-1543339469-94ba2391431f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60);
        font-family: 'Calistoga', cursive;        
}


h1 {
    color: rgb(230, 137, 16);
    font-weight: bolder;
}

    #submit-btn {
	padding: 0.57em 1em 0.57em 1em;
	font-size: 1.5em;
	background: rgb(230, 137, 16);
	border-radius: 10px;
	margin-left: -5px;
}

    #burger-name {
	padding: 0.5em 1em 0.5em 1em;
	font-size: 1.5em;
    border-radius: 10px;
    margin-left: 110px;
}
</style> 
 
 
 <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                {{!-- <div class="panel panel-default">
                    <div class="panel-heading"><h2 class="text-center">Place Order</h2></div>
                    <div class="panel-body"> --}}
                        <form class="create-update-form" action="burgers/create" method="POST">
                          <div class="form-group">
                            {{!-- <label for="ca">Burger Name:</label>
                            <input type="text" id="ca" name="name"> --}}
                            <input id="burger-name" type="text" name="burger_name" placeholder="Order &#127828; Here!">
			                <button id="submit-btn" type="submit">Place Order</button>
                          </div>

                          {{!-- <div class="form-group">
                            <label for="slee">Order Status</label><br>
                            <input type="radio" name="devoured" value="0" checked> Pick Up Order                            
                            <input type="radio" name="devoured" value="1"> Order Picked Up<br>
                          </div> --}}

                          {{!-- <button type="submit" class="btn-primary">Place Order</button> --}}
                        </form>
                    {{!-- </div>
                </div> --}}
            </div>
            <div class="col-md-3"></div>
            
        </div>

        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-4">
                <div class="panel panel-default">
                    <div class="panel-heading"><h2 class="text-center">Placed Order</h2></div>
                    <div class="panel-body">
                        <ol>
                          {{#each burgers}}
                            {{#unless this.devoured}}
                              <li>
                                {{this.burger_name}}

                                 {{!-- <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                                  <input type="hidden" name="devoured" value="true">
                                  <button type="submit">Devour it</button>
                                </form>  --}}
                                {{> burgers/pickup-order}}
                              </li>
                            {{/unless}}
                          {{/each}}
                        </ol>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="panel panel-default">
                    <div class="panel-heading"><h2 class="text-center">Devoured</h2></div>
                    <div class="panel-body">
                        <ol>
                          {{#each burgers}}
                            {{#if this.devoured}}
                              <li>
                                {{this.burger_name}}

                                 {{!-- <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                                  <input type="hidden" name="devoured" value="false">
                                </form>  --}}
                                {{> burgers/order-pickedup}}
                              </li>
                            {{/if}}
                          {{/each}}
                        </ol>
                    </div>
                </div>
            </div>
            <div class="col-md-2"></div>
        </div>